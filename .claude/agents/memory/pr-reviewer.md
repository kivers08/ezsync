# pr-reviewer — role-specific memory

Corrections specific to this agent's role/behavior, curated by the main context —
never self-written. Codebase-wide technical rules stay in `tasks/lessons.md`; see this
directory's `README.md` for the filing rule.

<!-- Authoritative copy. This agent reads only this spoke, so entries that bind THIS
     agent's role live here; `tasks/lessons.md` keeps only an Index tombstone pointing
     back. Any half of a lesson that binds the MAIN CONTEXT instead must stay in the hub
     or a `.claude/rules/` file — a spoke binds only its own reader.
     No entries yet — fresh project. Append curated entries below, newest first, in the
     canonical entry format defined in this directory's `README.md`. -->

# copywriter — role-specific memory

Corrections specific to this agent's role/behavior, curated by the main context —
never self-written. Codebase-wide technical rules stay in `tasks/lessons.md`; see this
directory's `README.md` for the filing rule.

<!-- No entries yet. Two former entries were dispatcher-audience (the actor was the main
     context, which never reads spokes) and moved to `.claude/skills/dispatch/SKILL.md`
     Step 2. The main context appends curated entries below, newest first, in the canonical
     entry format defined in this directory's `README.md` ("Format (binding for spokes)") —
     do not invent a different shape. -->

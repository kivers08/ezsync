# lessons

Active **main-context process lessons** — judgment and workflow corrections that bind the
coordinator (main context) and have no better home. Codebase-wide rules with a path
signature belong in `.claude/rules/*.md`; agent-role-specific notes belong in
`.claude/agents/memory/<agent>.md`. Routing contract:
`.claude/agents/memory/README.md`.

Read this file through the Index, then grep the heading you need — never read it in full
(`.claude/rules/task-files-conventions.md`). Superseded lessons demote to
`tasks/lessons-archive.md` (grep-only) with equal rigor; promote genuinely new ones here
only at `session-wrap-up`.

## Index

_(no lessons recorded yet)_

## Lessons

_(empty — real lessons land here as corrections accumulate)_
